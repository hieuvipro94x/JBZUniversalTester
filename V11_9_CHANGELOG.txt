JBZUniversalTester V11.9 - MASTER SAMPLE + PROBE SIGNATURE FIX
=============================================================

1. Sửa lỗi đầu dò GND vẫn bị hiểu thành hàng loạt lỗi đấu sai/chập trong TestView.
   - Nhận dạng chữ ký fan-out dày của que GND trước TestEngine.
   - Frame đầu dò không đi vào logic WrongWiring.
   - Chỉ hiển thị đúng IO đang chạm; IO không có THT vẫn hiện IO(n).
   - Bỏ popup và TESTPOINT.wav cho frame đầu dò.
   - Nhả đầu dò: frame production bình thường tiếp theo khôi phục bảng THT.

2. Master Sample Gate bắt buộc trước sản xuất.
   - TEST MASTER ĐẠT: phải hoàn thành continuity + điện trở (nếu model có) + PASS.
   - TEST MASTER LỖI: phải tạo lỗi dây/chập hoặc lỗi điện trở.
   - Hai master không cộng Tổng/PASS/FAIL và không tăng LOTNO.
   - Chỉ khi cả hai checkbox OK mới cho bấm XÁC NHẬN 2 MASTER.
   - Sau xác nhận mới mở khóa production bình thường.
   - Reset master khi đổi mã hàng hoặc khởi động lại ứng dụng.

3. Cấu hình card giữ quy tắc V11.8:
   - 1 logical scan card = 32 IO.
   - 1 card mở rộng = 2 logical card = 64 IO.
   - 1..10 card mở rộng = 64..640 IO.
