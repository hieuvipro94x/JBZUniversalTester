# MASTER TASK

# RE-ARCHITECT JBZUniversalTester THEO KIẾN TRÚC HTDRV GỐC

# MỤC TIÊU: UI REALTIME, KHÔNG LAG, KHÔNG FREEZE, KHÔNG DELAY CẢM NHẬN

Project: JBZUniversalTester

============================================================
I. MỤC TIÊU CHÍNH
=================

Hãy phân tích TOÀN BỘ solution hiện tại và tái cấu trúc phần:

* startup;
* D2XX/FTDI;
* scan worker;
* frame decoder;
* TestEngine;
* TestViewModel;
* MainViewModel;
* MainWindow;
* TestWindow;
* model loading;
* history/statistics;
* logging;
* Probe;
* Master;
* Resistance;
* Waterproof/Leak;
* relay;
* shutdown;

theo nguyên tắc kiến trúc của phần mềm Htdrv gốc.

KHÔNG yêu cầu copy source C++ của Htdrv.

Yêu cầu là copy CẤU TRÚC VẬN HÀNH / LOGIC THREADING của bản gốc:

1. Một hardware owner duy nhất.
2. Một FTDI connection duy nhất.
3. Một scanner worker duy nhất.
4. Scanner sống lâu trong suốt vòng đời app.
5. D2XX event-driven / wait khi không có data.
6. Hardware worker hoàn toàn độc lập UI.
7. Parser hoàn toàn độc lập UI.
8. Test state machine không chạy trên WPF Dispatcher.
9. UI chỉ nhận logical state change.
10. Bấm START chỉ ARM test state.
11. Không STOP/START hardware khi không thật sự cần.
12. History/statistics không nằm trong hot path.
13. Không spam Dispatcher.
14. Không spam ObservableCollection.
15. Không dùng delay lớn để che lỗi kiến trúc.

Mục tiêu cảm nhận:

* mở app nhanh;
* MainWindow hiện ngay;
* không Not Responding;
* chọn mã hàng không đơ;
* bấm BẮT ĐẦU gần như tức thì;
* TestWindow mở mượt;
* probe phản hồi nhanh;
* short/wrong wiring phản hồi nhanh;
* PASS/FAIL không giật UI;
* chạy production nhiều giờ không tích tụ Task/thread/Dispatcher callback.

============================================================
II. CÁC ĐẶC ĐIỂM ĐÃ XÁC NHẬN TỪ HTDRV GỐC
=========================================

Phần mềm Htdrv gốc là native Win32/MFC 32-bit.

Kiến trúc quan sát được:

UI MESSAGE THREAD
|
+-- GetMessage / PeekMessage
+-- button
+-- draw
+-- UpdateWindow/Invalidate
+-- nhận notification từ worker

HARDWARE WORKER
|
+-- FTDI wait/event
+-- GetQueueStatus
+-- Read
+-- Decode
+-- State machine
+-- gửi logical result sang UI

HTDRV có sử dụng:

CreateThread / worker thread
CreateEvent
WaitForSingleObject
WaitForMultipleObjects
PostMessage
PeekMessage
SetTimer

D2XX flow quan sát được sử dụng:

FT_OpenEx
FT_Close
FT_SetEventNotification
FT_GetQueueStatus
FT_Read
FT_Write
FT_Purge

Có đăng ký:

FT_EVENT_RXCHAR

HTDRV không busy-spin vô hạn.

Có wait/sleep ngắn trong hardware worker, ví dụ logic tương đương:

while (running)
{
wait/yield;

```
if (RX empty)
{
    wait;
    continue;
}

read/process;
```

}

QUAN TRỌNG:

Delay/wait này nằm trong HARDWARE WORKER.

KHÔNG phải UI delay.

Do đó:

Sleep/Wait worker != UI freeze.

Không được hiểu sai rồi xóa tất cả wait.

============================================================
III. KIẾN TRÚC ĐÍCH BẮT BUỘC
============================

Chuyển hệ thống thành:

```
                    WPF UI
                      ▲
                      │
                UiStateDelta
                      │
              UI coordinator
                      ▲
                      │
               logical change
                      │
                TestEngine
                      ▲
                      │
                ScanFrame
                      │
               FrameDecoder
                      ▲
                      │
                 raw byte
                      │
            D2XX RX WORKER
                      ▲
                      │
                 FT245R
```

Cụ thể:

FT245R
↓
D2xxBoardTransport worker
↓
BoardIoDecoder
↓
ScanFrame
↓
TestEngine
↓
TestStateSnapshot / StateDelta
↓
TestViewModel
↓
WPF UI

Không được:

FT_Read
↓
Dispatcher
↓
ViewModel
↓
DataGrid

Không được:

raw byte
↓
UI.

============================================================
IV. TẠO MỘT HARDWARE OWNER DUY NHẤT
===================================

Rà soát toàn solution.

Các class quan trọng:

D2xxBoardTransport
UnifiedBoardTransport
ScanSupervisor
TestEngine
TestViewModel
MainViewModel

Sau refactor phải đảm bảo:

D2xxBoardTransport

là class DUY NHẤT trực tiếp gọi:

FT_OpenEx
FT_Close
FT_Read
FT_Write
FT_GetQueueStatus
FT_SetEventNotification
FT_Purge
FT_SetLatencyTimer
FT_SetUSBParameters
FT_SetTimeouts

Không class khác được trực tiếp dùng D2XX.

Nếu tìm được:

FT_Open
FT_OpenEx
FT_Close

ở bất kỳ class khác:

phải đánh giá và loại bỏ duplicate lifecycle nếu không thật sự là diagnostic độc lập.

============================================================
V. MỘT D2XX CONNECTION CHO TOÀN APP
===================================

Flow mới:

Application start
↓
create board service
↓
ConnectAsync()
↓
FT_OpenEx đúng một lần
↓
configure D2XX
↓
handshake
↓
prepare firmware
↓
start permanent RX worker

Connection này được giữ cho tới khi:

* USB fault nghiêm trọng;
* operator yêu cầu reconnect;
* app shutdown.

Không đóng/mở FTDI vì:

* đổi Window;
* chọn model;
* mở TestWindow;
* đóng TestWindow;
* bắt đầu cycle;
* kết thúc cycle;
* PASS;
* đổi UI tab.

============================================================
VI. CONNECTION STATE MACHINE
============================

Thiết kế explicit state:

Disconnected
Connecting
Initializing
Ready
Scanning
PausedForHardwareOperation
Recovering
Faulted
ShuttingDown

State transition phải serialize.

Ví dụ:

Disconnected
→ Connecting
→ Initializing
→ Ready
→ Scanning

Không để:

Connecting
→ Connecting

hoặc:

Scanning
→ StartScanning
→ tạo worker thứ hai.

============================================================
VII. CONNECTASYNC PHẢI IDEMPOTENT
=================================

Nếu:

Connected == true

ConnectAsync phải return ngay.

Nếu ConnectAsync đang chạy:

caller khác phải await SAME TASK hoặc gate.

Không chạy Connect song song.

Có thể dùng:

SemaphoreSlim lifecycleGate

và cached connect task.

Không dùng:

lock
{
await ...
}

============================================================
VIII. MỘT PERMANENT RX WORKER
=============================

Đây là thay đổi quan trọng nhất.

Không coi StartProductionTest == StartHardwareReader.

Hardware reader phải sống độc lập.

Sau connection:

StartReaderWorker()

một lần.

Worker chỉ kết thúc khi:

disconnect
USB error
shutdown.

Không kết thúc reader vì:

Stop Test
PASS
FAIL
Window close.

============================================================
IX. D2XX EVENT-DRIVEN RX
========================

Dùng:

FT_SetEventNotification(
handle,
FT_EVENT_RXCHAR,
rxEvent
)

Worker:

while (!ct.IsCancellationRequested)
{
wait RX event hoặc cancellation;

```
GetQueueStatus;

while (available > 0)
{
    read một block hợp lý;

    Feed decoder;

    đọc tiếp nếu driver vẫn còn backlog;
}
```

}

Không:

while(true)
{
FT_GetQueueStatus(...)
}

hàng nghìn lần/s.

============================================================
X. WAIT STRATEGY GIỐNG TƯ DUY HTDRV
===================================

Không được busy-spin.

Có thể:

WaitHandle.WaitAny(
rxEvent,
cancellation,
timeout
)

Timeout chỉ phục vụ:

watchdog
perf metrics
USB state.

Khi queue empty:

wait event.

Nếu event notification có vấn đề với driver:

fallback polling rate ngắn hợp lý.

Ví dụ 1-10ms trong HARDWARE WORKER.

Nhưng phải benchmark.

KHÔNG đặt delay này trong UI.

============================================================
XI. D2XX READ BLOCK
===================

Không cố read 1-2 byte nếu queue đã có nhiều data.

Pseudo:

GetQueueStatus(out available)

if available > 0:
readCount = min(available, buffer.Length)

FT_Read(
buffer,
readCount
)

Feed toàn block vào BoardIoDecoder.

BoardIoDecoder phải chịu được:

partial frame
multiple frames trong 1 Read
frame split qua nhiều Read.

============================================================
XII. FRAME DECODER PHẢI LÀ PURE HOT PATH
========================================

BoardIoDecoder không được:

* Dispatcher;
* MessageBox;
* database;
* logging file sync;
* ObservableCollection;
* audio;
* WPF.

Input:

ReadOnlySpan<byte>

Output:

0..N ScanFrame.

Tối ưu allocation nếu cần.

Tránh ToHashSet/ToDictionary vô ích ở mọi partial state nếu profiler cho thấy nặng.

Nhưng KHÔNG rewrite trước khi benchmark.

============================================================
XIII. HARDWARE READER KHÔNG BIẾT UI
===================================

D2xxBoardTransport chỉ phát:

FrameReceived

hoặc channel/queue.

Không biết:

TestWindow
FaultGrid
State color
MessageBox
DataGrid
ProbeBar.

============================================================
XIV. TESTENGINE PHẢI LÀ LOGICAL STATE MACHINE
=============================================

TestEngine nhận ScanFrame.

TestEngine xử lý:

* continuity;
* product activity;
* wrong wiring;
* short;
* stable frame confirmation;
* product removal;
* expected network;
* Master;
* pass gate logic cần thiết.

Không được TestEngine trực tiếp update WPF.

Output nên là:

TestEngineState

hoặc:

TestStateDelta.

Ví dụ:

record TestStateDelta
{
bool StateChanged;
bool FaultsChanged;
bool ProbeChanged;
bool ProductDetectedChanged;
bool ContinuityChanged;
...
}

UI chỉ update những gì changed.

============================================================
XV. WPF CHỈ NHẬN STATE CHANGE
=============================

Không update UI cho mọi ScanFrame.

Ví dụ:

Frame #100:
same state

→ KHÔNG update UI.

Frame #101:
same state

→ KHÔNG update UI.

Frame #102:
WrongWiring confirmed

→ update fault UI.

Frame #103:
same confirmed wrong

→ không rebuild grid.

============================================================
XVI. UI COALESCING
==================

Tạo một UI update coordinator.

Không:

dispatcher.BeginInvoke()
cho từng event nhỏ.

Dùng:

pending state
+
single scheduled flush.

Pseudo:

if (Interlocked.Exchange(ref uiPending, 1) == 0)
{
Dispatcher.BeginInvoke(
FlushUiState,
DispatcherPriority.Background);
}

Trong Flush:

* đọc latest snapshot;
* cập nhật properties thật sự đổi;
* sync collections thật sự đổi;
* clear uiPending.

Nếu state đổi lại trước khi Flush:

chỉ cần latest snapshot.

============================================================
XVII. UI UPDATE RATE
====================

Hardware xử lý mọi frame.

UI không cần render 14-100 lần/s nếu trạng thái không đổi.

Cho phép UI refresh tối đa khoảng:

30-60Hz

nếu cần animation/value.

Nhưng fault detection không throttle.

Không bỏ frame.

============================================================
XVIII. LOG KHÔNG NẰM TRONG HOT PATH UI
======================================

Raw D2XX logs:

RX
TX
frame

phải đi async logger.

Không:

FT_Read
→ file write sync.

Không:

FrameReceived
→ Logs.Insert trên Dispatcher ngay.

UI log:

pending queue
→ batch flush.

Giới hạn ví dụ:

300 dòng UI.

Diagnostic raw logging có thể rất nhiều nhưng không được làm UI lag.

============================================================
XIX. BẮT CHƯỚC HTDRV VỀ UI MESSAGE PHILOSOPHY
=============================================

Win32 gốc dùng tư duy:

worker làm việc
↓
PostMessage khi cần
↓
UI xử lý notification ngắn.

WPF tương đương:

worker
↓
StateDelta
↓
single Dispatcher.BeginInvoke
↓
small UI apply.

Không dùng Dispatcher như một job queue cho toàn bộ hardware pipeline.

============================================================
XX. STARTUP FLOW MỚI
====================

Flow bắt buộc:

App.OnStartup
↓
tạo MainWindow
↓
Show
↓
ContentRendered
↓
UI ready
↓
deferred initialization
↓
Board.ConnectAsync background
↓
hardware READY
↓
start permanent RX worker
↓
load last model async
↓
set logical model
↓
stats/history background
↓
ready

Không heavy work trước first render.

============================================================
XXI. APP.ONSTARTUP PHẢI NHẸ
===========================

Audit:

StartupBootstrapService
ProductionConfigService
AsyncFileLogService
AppSoundService

Chỉ giữ việc bắt buộc.

Audio:

defer.

Large filesystem/database migration:

defer nếu có thể.

Không load history trước MainWindow render.

============================================================
XXII. MAINVIEWMODEL CONSTRUCTOR
===============================

Constructor chỉ:

* load config nhỏ;
* tạo services;
* tạo ViewModels;
* bind commands.

Không:

* connect FTDI;
* scan history;
* parse model;
* create filesystem watchers lớn;
* initialize hardware;
* COM discovery.

============================================================
XXIII. TESTVIEWMODEL CONSTRUCTOR
================================

Phải lightweight.

Không:

AppSoundService.Initialize
legacy history scan
FileSystemWatcher initialization trên UI
model parse
database scan
D2XX connect.

Subscriptions lightweight được phép.

============================================================
XXIV. AUDIO
===========

AppSoundService là application singleton.

Initialize một lần.

Không:

App.Initialize
+
TestViewModel.Initialize

hai lần.

Audio init/play startup chạy sau first render hoặc worker thích hợp.

============================================================
XXV. MODEL SELECTION PHILOSOPHY
===============================

HTDRV-style:

Chọn model
↓
parse model
↓
replace logical configuration
↓
ready.

Không:

Chọn model
↓
hardware reconnect
↓
stop scan
↓
history scan
↓
database scan
↓
watchers
↓
full UI rebuild
↓
start scan.

============================================================
XXVI. THT PARSER BACKGROUND
===========================

ThtModelParser.Load chạy Task.Run/background.

Sau parse:

apply model logic ngắn.

Nếu `TestEngine.SetModel()` nặng:

hãy profile.

Có thể build immutable model state ở background trước.

============================================================
XXVII. MODEL GENERATION TOKEN
=============================

Nếu người dùng chọn:

A
rồi B

trong lúc A chưa parse xong:

A không được ghi đè B.

Dùng:

_modelGeneration.

Mỗi load:

generation++.

Commit chỉ nếu generation vẫn đúng.

============================================================
XXVIII. HISTORY/STATISTICS HOÀN TOÀN TÁCH MODEL HOT PATH
========================================================

Không để:

SetModel()
→ LegacyPhtHistoryReader.GetProductionSnapshot()
→ scan C:\Pass_
→ scan C:\Error_
→ UI đứng.

Flow mới:

SetModel logical
↓
UI model ready
↓
background statistics task
↓
kết quả statistics
↓
small UI update.

Statistics không được trì hoãn khả năng Start Test.

============================================================
XXIX. LÂU DÀI: CACHE / INDEX HISTORY
====================================

Nếu `LegacyPhtHistoryReader` đang đọc toàn bộ:

Day*.dat
Day*.err

mỗi lần chọn model:

hãy thiết kế cache/index.

Có thể:

* cache theo file timestamp;
* chỉ parse file thay đổi;
* maintain aggregate;
* hoặc query TestHistory database.

Không cần rewrite ngay nếu background đủ tốt, nhưng phải loại bỏ repeated full scans nếu hiện đang xảy ra.

============================================================
XXX. FILESYSTEMWATCHER
======================

Tạo sau UI startup.

Chỉ một bộ watcher.

Callbacks:

debounce.

Không đọc toàn bộ history ngay mỗi event filesystem.

Dùng:

change flag
+
timer/debounce
+
background refresh.

============================================================
XXXI. MAINWINDOW -> TESTWINDOW
==============================

Bấm BẮT ĐẦU KIỂM TRA không được reconnect hardware.

Điều kiện:

board connected
+
permanent scanner running
+
model ready

thì:

Open TestWindow
↓
ARM logical Production state
↓
return.

Mục tiêu cảm nhận gần tức thì.

============================================================
XXXII. TESTWINDOW LOADED
========================

Không:

await InitializeHardwareAsync

nếu hardware đã được Main startup initialize.

Nếu board chưa ready:

TestWindow có thể hiển thị:

ĐANG KẾT NỐI

nhưng UI vẫn responsive.

Không block render.

============================================================
XXXIII. START TEST PHẢI CHỈ ARM LOGIC
=====================================

Đây là thay đổi cốt lõi.

Old-style không mong muốn:

StartTestAsync
↓
AllRelaysOff
↓
ConfigureScanRange
↓
StopScan
↓
StartScan
↓
wait frame
↓
ARM.

New:

StartTestAsync
↓
validate board/model
↓
reset logical cycle state
↓
capture current frame generation
↓
enable TestEngine processing
↓
ARM Production
↓
return.

Không chạm hardware nếu không cần.

============================================================
XXXIV. BACKGROUND SCAN CHẠY LIÊN TỤC
====================================

Ngay khi FTDI ready:

scan Production background.

MainWindow:
scan vẫn chạy.

TestWindow:
scan vẫn chính stream đó.

Close TestWindow:
scan vẫn chạy.

Open lại:
reuse.

============================================================
XXXV. FRESH FRAME GATE
======================

Khi ARM một cycle mới:

không cần restart hardware để lấy "frame sạch".

Thay vào đó:

capture:

LastFrameSequence

sau ARM chỉ chấp nhận frame:

Sequence > armSequence.

Đây là cách logical generation thay hardware restart.

============================================================
XXXVI. SCAN GENERATION
======================

Transport generation chỉ dùng khi:

thực sự restart stream hardware.

Cycle generation dùng riêng.

Không dùng hardware restart chỉ để đổi cycle generation.

============================================================
XXXVII. SCANSUPERVISOR
======================

ScanSupervisor chỉ làm:

* ensure reader/scan running;
* watchdog;
* stall recovery.

Không dùng như helper để STOP/START tại mọi phase.

EnsureProductionScanAsync:

if healthy production scan:
return false.

============================================================
XXXVIII. WATCHDOG
=================

Nếu no frame quá threshold:

LEVEL 1:
check scanner state.

LEVEL 2:
STOP/START firmware scan một lần.

LEVEL 3:
disconnect/reconnect D2XX.

LEVEL 4:
DeviceFault.

Không reconnect vì UI transition.

============================================================
XXXIX. CONFIGURESCANRANGE
=========================

Không gọi native driver API nếu config không đổi.

Cache:

currentCapacity
currentLatency
currentStartCard
currentUsbDelay.

Nếu same:

return.

Nếu cần FT_SetLatencyTimer:

thực hiện async serialized.

Không `_ioLock.Wait()` trên UI thread.

============================================================
XL. LOẠI BỎ SYNCHRONOUS LOCK TRÊN UI
====================================

Search:

.Wait()
.Result
.GetAwaiter().GetResult()
Thread.Sleep
_ioLock.Wait()
Dispatcher.Invoke

Mỗi trường hợp phải audit.

Public async path dùng:

WaitAsync.

============================================================
XLI. RESISTANCE
===============

Resistance là hardware mode đặc biệt.

Được phép:

logical Production pause
↓
STOP_SCAN firmware nếu protocol cần
↓
route 0x90/0x91
↓
measure
↓
release route
↓
START_SCAN

Nhưng:

D2XX CONNECTION vẫn mở.

RX worker có thể pause/wait theo transport state.

Không dispose worker/connection.

Sau resistance:

restart stream đúng một lần.

============================================================
XLII. WATERPROOF
================

Waterproof COM là subsystem riêng.

Không để COM leak block D2XX thread.

Không để leak SerialPort run trên UI.

D2XX và Waterproof phải độc lập.

Nếu leak stage cần pause D2XX firmware scan:

pause có chủ đích.

Không disconnect FTDI.

============================================================
XLIII. RELAY
============

Relay command không được tự invalidate toàn bộ scan prepare nếu firmware không yêu cầu.

Xác minh command:

0x8E

không làm `_scanPrepared=false` trừ khi trace hardware chứng minh.

Resistance routing:

0x90
0x91

có thể invalidate prepared.

Phân biệt rõ.

============================================================
XLIV. PASS FLOW
===============

Sau continuity PASS:

logical state
↓
optional resistance
↓
optional waterproof
↓
marking/jig
↓
restart Production scan nếu hardware stage đã pause
↓
wait product removal.

Không restart hardware nếu không pause.

============================================================
XLV. FAIL FLOW
==============

Wrong/Short confirmed:

khóa logical cycle
↓
UI Fault
↓
optional stop firmware scan nếu relay/eject cần
↓
confirm
↓
jig relay
↓
resume scan
↓
wait removal.

Không reconnect D2XX.

============================================================
XLVI. PROBE
===========

Probe observer không được:

Stop Production
Start Probe
Stop Probe
Start Production

nếu protocol Production hiện tại đã cung cấp đủ signature Probe.

Giữ observer logic như hiện tại nếu đã xác nhận.

Probe detection chạy ngoài UI.

UI chỉ nhận:

TOUCH IO list
RELEASE.

============================================================
XLVII. PROBE UI
===============

Không:

Clear collection
Add
Clear
Add

mỗi frame.

Chỉ khi contact set thay đổi.

Dùng set comparison:

old probe IO
vs
new probe IO.

============================================================
XLVIII. WRONG / SHORT
=====================

Không throttle TestEngine.

Mọi complete production frame vẫn đi qua fault engine.

Confirmation gate giữ như hiện tại.

UI chỉ render khi candidate/confirmed logical state thay đổi.

============================================================
XLIX. OBSERVABLECOLLECTION STRATEGY
===================================

Các collection:

Faults
ProbeContacts
Resistance
WaterProofChannels
MasterFaults
Cards
Logs

phải tránh mutation vô ích.

Tạo helper:

SynchronizeRows(existing, desired)

diff bằng stable key.

Chỉ:

Add
Remove
Update

row thay đổi.

============================================================
L. FAULTGRID
============

Không rebuild toàn DataGrid nếu:

fault signature không đổi.

Tạo:

faultDisplaySignature.

Nếu same:
return.

============================================================
LI. CARD UI
===========

Cards không rebuild mỗi frame.

Build khi:

capacity changed.

Probe chỉ update:

HasProbeActivity

cho card liên quan.

============================================================
LII. STATE PROPERTY
===================

Không Raise:

StateBackground
StateForeground
ResultStatusText
...

nếu State value không đổi.

Set helper đã hỗ trợ thì tận dụng.

============================================================
LIII. MASTER
============

Giữ toàn bộ semantics Master hiện tại.

Nhưng state machine Master xử lý logical state, không điều khiển UI trực tiếp ở hot path.

MasterFault collection chỉ update khi unique fault set đổi.

============================================================
LIV. MESSAGEBOX
===============

Không MessageBox từ hardware worker.

Worker phát event:

FaultConfirmed.

UI layer quyết định popup.

Nếu flow hiện tại khó refactor hết:

ít nhất Dispatcher.BeginInvoke popup và không giữ D2XX locks.

============================================================
LV. AUDIO EVENTS
================

Hardware/TestEngine phát logical event:

ProbeTouch
WiringFaultStart
Pass
Fail

Audio service xử lý riêng.

Không audio file I/O trong frame callback.

============================================================
LVI. LOGGING
============

AsyncFileLogService phải thật sự async.

Không log mỗi unchanged frame ở Normal level.

Raw RX:

Diagnostic only.

Production log:

state transition.

============================================================
LVII. PERFORMANCE COUNTERS
==========================

Thêm:

D2XX opens
D2XX closes
reader workers
queue calls/sec
reads/sec
bytes/sec
frames/sec
engine frames/sec
logical changes/sec
UI flush/sec
Dispatcher stalls
GC allocation nếu dễ đo.

Mỗi 5 giây summary.

============================================================
LVIII. UI HEARTBEAT
===================

Thêm lightweight Dispatcher heartbeat khi diagnostics enabled.

Ví dụ:

250ms expected.

Nếu actual > expected + 100ms:

UI_STALL duration_ms=...

Không popup.

============================================================
LIX. TIMING MARKERS
===================

Startup:

APP_START
MAIN_CONSTRUCTED
FIRST_RENDER
BOARD_CONNECT_BEGIN
BOARD_OPENED
HANDSHAKE_DONE
SCANNER_RUNNING
MODEL_PARSE_BEGIN
MODEL_PARSE_DONE
MODEL_APPLIED
STATS_BACKGROUND_BEGIN
STARTUP_READY

Model:

MODEL_SELECT_CLICK
MODEL_PARSE_BEGIN
MODEL_PARSE_DONE
ENGINE_MODEL_READY
MODEL_UI_READY
HISTORY_READY

Test:

TEST_START_CLICK
TEST_ARM_BEGIN
TEST_ARM_READY
FIRST_FRAME_AFTER_ARM
FIRST_LOGICAL_CHANGE
FIRST_UI_RENDER.

============================================================
LX. NO ALLOCATION HOT-PATH AUDIT
================================

Profile:

BoardIoDecoder
TestEngine.ProcessFrame
ProbeContactClassifier.

Search excessive:

ToArray()
ToHashSet()
ToDictionary()
OrderBy()
string.Join()
LINQ

trong every-frame path.

Không rewrite tất cả.

Chỉ tối ưu nơi profiler/hot path cho thấy cần.

============================================================
LXI. THREADING MODEL CUỐI
=========================

Expected threads/tasks:

1 UI Dispatcher

1 D2XX reader worker

optional:
1 async logger worker

optional:
Waterproof task khi chạy

optional:
history/stat task khi cần

Không:

mỗi frame tạo Task.Run.

Không:

mỗi frame tạo Dispatcher task nhiều lần.

============================================================
LXII. CHANNEL OPTION
====================

Nếu phù hợp .NET version hiện tại, có thể dùng:

System.Threading.Channels

giữa:

D2XX reader
→ frame consumer.

Ví dụ bounded/unbounded phù hợp.

Nhưng không được drop ScanFrame production.

Nếu không cần Channel thì event direct worker -> engine cũng được.

Ưu tiên đơn giản.

============================================================
LXIII. D2XX LOCK ORDER
======================

Canonical:

lifecycleGate
→ scanSwitchGate
→ ioGate

nhưng không giữ gate qua UI operation.

Rà soát deadlock.

Không:

ioLock held
→ Dispatcher.

============================================================
LXIV. DISCONNECT
================

Disconnect:

transition ShuttingDown/Disconnected
↓
STOP firmware
↓
cancel reader
↓
await reader
↓
relay safe off
↓
optional final firmware cleanup
↓
FT_Close ONCE.

============================================================
LXV. DISPOSE
============

UnifiedBoardTransport.DisposeAsync không được cleanup hai lần.

Inner D2xx Dispose idempotent.

MainViewModel không cần:

Test.Shutdown
→ board disconnect

rồi:

board Dispose
→ disconnect hardware lại

nếu first path đã cleanup.

Thiết kế rõ owner.

============================================================
LXVI. ERROR RECOVERY
====================

Nếu FT_Read lỗi:

reader worker:
mark connection fault
↓
close stale handle
↓
notify supervisor
↓
reconnect controlled.

Không tự reconnect đồng thời từ:

reader
hardware monitor
TestView
MainWindow.

Một recovery coordinator.

============================================================
LXVII. HARDWARE MONITOR
=======================

Hardware monitor không trực tiếp gọi reconnect nếu reconnect đang chạy.

Dùng:

EnsureConnectedAsync

idempotent.

Monitor chỉ phát request.

============================================================
LXVIII. REMOVE DUPLICATE INITIALIZATION
=======================================

Search toàn solution:

InitializeHardwareAsync
InitializeAsync
ConnectBoardAsync
ConnectAsync
StartScanAsync
StartProductionScanAndVerifyFrameAsync

Lập call graph.

Loại mọi đường duplicate.

============================================================
LXIX. SEARCH FT_OPEN INDEX
==========================

Trace từng có FT_Open(index).

Search:

FT_Open
DllImport
GetProcAddress
ordinal D2XX
third-party wrapper

Nếu source project không chứa:

ghi rõ kết luận.

Không tự thêm logic đoán.

============================================================
LXX. KHÔNG CỐ BẮT CHƯỚC NATIVE C++ BẰNG CÁCH DÙNG THREAD.SLEEP UI
=================================================================

HTDRV dùng Sleep/Wait ở worker.

Không sửa WPF thành:

Thread.Sleep(10)

trên UI.

Đây là lỗi nghiêm trọng.

============================================================
LXXI. TESTWINDOW
================

TestWindow constructor:

InitializeComponent
bind DataContext
light setup.

ContentRendered xảy ra nhanh.

Loaded không làm:

hardware initialization dài

nếu Main đã init.

============================================================
LXXII. MODEL UI
===============

Khi model parse xong:

ModelName
PartNumber
pins
fault baseline

hiện nhanh.

Daily statistics có thể load sau.

Không đợi history để enable Start Test.

============================================================
LXXIII. SAVE LAST MODEL
=======================

Không synchronous ghi appsettings trong TEST hot path nếu không cần.

Nếu SaveLastTestedModel chỉ khi model path đổi:

cache last saved path.

Không ghi JSON mỗi cycle nếu same model.

============================================================
LXXIV. PRODUCTION SETTINGS
==========================

Không Reload ProductionSettings mỗi lần Start nếu file không đổi.

Settings page Save:
apply once.

Có config version.

============================================================
LXXV. DATABASE
==============

SQLite/history write sau PASS/FAIL phải đo.

Nếu write có thể lâu:

result logical state commit trước.

Database I/O không được giữ Dispatcher.

Nhưng consistency result phải đảm bảo.

============================================================
LXXVI. LABEL
============

Label printing async.

Không block scan worker.

PASS state không bị mất nếu printer chậm.

Print failure là label fault, không hardware scan fault.

============================================================
LXXVII. PRODUCTION RESULT COMMIT
================================

Mỗi cycle:

Interlocked result gate

chỉ commit một lần.

Giữ semantics hiện tại.

============================================================
LXXVIII. DO NOT BREAK THESE FUNCTIONS
=====================================

Bắt buộc giữ:

Correct wiring PASS

Wrong wiring FAIL

Short FAIL

OPEN không phải Product FAIL nếu logic hiện tại đã disable OPEN

Resistance

Waterproof

Master Good

Master Bad unique fault count

Probe

CLIP topology

Product removal

Relay jig

Marking

Label

History

Counter

Manual mode.

============================================================
LXXIX. ACCEPTANCE: STARTUP
==========================

App launch:

MainWindow render trước heavy work.

UI có thể:

drag
minimize
click

trong lúc board connect.

No Not Responding.

Normal startup:

D2XX Open logical count = 1.

Reader worker = 1.

INIT sequence = 1.

============================================================
LXXX. ACCEPTANCE: MODEL
=======================

Chọn file `.tht`:

dialog close
↓
UI tiếp tục responsive
↓
parse background
↓
model applied
↓
ready.

History có 10 năm dữ liệu vẫn không freeze UI.

============================================================
LXXXI. ACCEPTANCE: START TEST
=============================

Board already ready:

Start click
↓
ARM logical test
↓
near immediate.

Expected log:

TEST_START_SCAN_REUSED

Không:

STOP_SCAN
INIT_1
INIT_2
START_SCAN

chỉ vì bấm Start.

============================================================
LXXXII. ACCEPTANCE: LONG RUN
============================

Test >= 500 cycles hoặc stress simulation.

Không:

task count tăng mãi
thread count tăng mãi
handle tăng mãi
memory tăng tuyến tính
Dispatcher queue tăng mãi.

============================================================
LXXXIII. ACCEPTANCE: CPU
========================

Board idle:

D2XX worker gần như ngủ/wait.

Không 1000+ empty polls/sec.

Board scanning:

CPU hợp lý.

Không busy loop.

============================================================
LXXXIV. ACCEPTANCE: DETECTION LATENCY
=====================================

Không làm chậm nhận Pin.

Hardware frame khoảng 70ms nếu board thực tế như trace.

Logical detection không được thêm delay lớn ngoài confirmation frames hiện tại.

Probe touch/release nhanh.

============================================================
LXXXV. ACCEPTANCE: UI
=====================

Không rebuild DataGrid every frame.

Không Dispatcher callback every FT_Read.

Không Logs.Insert every RX callback trực tiếp.

UI state change mượt.

============================================================
LXXXVI. BUILD
=============

Sau refactor:

Clean
delete bin/obj
Restore
Build Release x64

hoặc target hiện tại project đang dùng.

Không thay architecture build nếu không cần.

============================================================
LXXXVII. TEST
=============

Nếu có hardware:

chạy thực tế.

Nếu không hardware:

tạo replay/simulation từ ScanFrame/raw trace để test:

reader-independent parser
engine
UI state delta.

Không mock logic khác thực tế.

============================================================
LXXXVIII. TRƯỚC KHI EDIT
========================

Trước khi sửa, in:

ARCHITECTURE AUDIT

Current FTDI owner:
...

Current reader workers:
...

Current scan lifecycle:
...

Current startup flow:
...

Current model flow:
...

Current Start Test flow:
...

Current UI update path:
...

Current history/stat path:
...

Major UI blockers:
1.
2.
3.

Duplicate lifecycle paths:
...

Files to modify:
...

============================================================
LXXXIX. SAU KHI EDIT
====================

In:

FILES MODIFIED

...

ARCHITECTURE BEFORE

...

ARCHITECTURE AFTER

...

THREAD MODEL

UI:
...

D2XX:
...

Engine:
...

History:
...

Logger:
...

============================================================
XC. PERFORMANCE REPORT
======================

Report nếu đo được:

First render ms

Board connect ms

Handshake ms

First frame ms

Model parse ms

Model UI apply ms

History load ms

Test ARM ms

D2XX GetQueueStatus/sec

FT_Read/sec

Frames/sec

UI flush/sec

Thread count

Handle count

Memory.

============================================================
XCI. REGRESSION MATRIX
======================

Board connect: PASS/FAIL

Background scan: PASS/FAIL

Start test reuse: PASS/FAIL

Correct harness: PASS/FAIL

Wrong wiring: PASS/FAIL

Short: PASS/FAIL

Probe: PASS/FAIL

Probe release: PASS/FAIL

CLIP: PASS/FAIL

Resistance: PASS/FAIL

Leak: PASS/FAIL

Leak equipment error: PASS/FAIL

Master Good: PASS/FAIL

Master Bad: PASS/FAIL

PASS relay: PASS/FAIL

FAIL jig: PASS/FAIL

Label: PASS/FAIL

History: PASS/FAIL

Counters: PASS/FAIL

Product removal: PASS/FAIL

Manual: PASS/FAIL

Shutdown: PASS/FAIL.

============================================================
XCII. QUY TẮC REFACTOR
======================

Không rewrite giao diện.

Không đổi `.tht`.

Không đổi protocol firmware.

Không đổi relay mapping.

Không đổi fault semantics.

Không đổi history schema nếu không cần.

Không đổi label format.

Không thêm artificial delay vào UI.

Không tạo backup file trong source tree.

============================================================
XCIII. NGUYÊN TẮC KIẾN TRÚC CUỐI
================================

Hãy coi Htdrv gốc là một ứng dụng:

PERMANENT HARDWARE ENGINE
+
THIN UI.

JBZ hiện tại cũng phải trở thành:

PERMANENT HARDWARE ENGINE
+
LOGICAL TEST STATE MACHINE
+
THIN WPF UI.

Không được tiếp tục thiết kế kiểu:

UI action
→ control hardware
→ start thread
→ wait
→ update UI.

Phải là:

hardware luôn chạy
↓
logical state luôn sẵn sàng
↓
UI chỉ điều khiển mode/ARM
↓
worker phát logical result.

============================================================
XCIV. FLOW CUỐI MONG MUỐN
=========================

APP START:

MainWindow render
↓
Board connect
↓
D2XX reader alive
↓
Production scan alive
↓
model load
↓
ready

SELECT MODEL:

parse async
↓
replace model logic
↓
ARM model
↓
stats async
↓
ready

START:

ARM Cycle
↓
capture frame sequence
↓
enable engine
↓
ready

PRODUCT INSERT:

frames
↓
engine
↓
continuity state

FAULT:

engine confirms fault
↓
one UI delta
↓
fault dialog

PASS:

continuity
↓
resistance/leak if configured
↓
relay
↓
resume same hardware system
↓
wait removal

REMOVE:

frame confirms no product
↓
reset logical cycle
↓
ARM next cycle

APP EXIT:

stop firmware
↓
cancel reader
↓
safe outputs
↓
FT_Close once.

============================================================
XCV. MỤC TIÊU CUỐI CÙNG
=======================

Sau task này, người vận hành phải cảm nhận:

* phần mềm mở lên không treo;
* không phải chờ giao diện “hồi lại”;
* chọn mã nhanh;
* TestWindow bật nhanh;
* bấm Start không delay;
* lắp sản phẩm phản ứng ngay theo tốc độ board;
* chập/sai dây hiện ngay sau confirmation cần thiết;
* probe chạm/nhả mượt;
* PASS/FAIL không khóa giao diện;
* chạy liên tục nhiều giờ vẫn ổn định.

Hãy ưu tiên KIẾN TRÚC ĐÚNG thay vì vá từng điểm lag.

Nếu phát hiện một thay đổi trong yêu cầu trên có nguy cơ làm thay đổi protocol hoặc logic production đã được xác minh, KHÔNG tự ý làm.

Hãy ghi rõ:

BLOCKED BY HARDWARE/PROTOCOL EVIDENCE

và giữ behavior cũ tại vị trí đó.

Những phần có đủ bằng chứng kiến trúc thì sửa triệt để.

============================================================
XCVI. QUAN TRỌNG NHẤT
=====================

BẤM START KHÔNG CÓ NGHĨA START HARDWARE.

START chỉ có nghĩa:

ARM LOGICAL TEST.

Hardware D2XX phải đã:

CONNECTED
+
INITIALIZED
+
SCANNING

từ trước.

Đây là nguyên tắc trung tâm của toàn bộ refactor.
