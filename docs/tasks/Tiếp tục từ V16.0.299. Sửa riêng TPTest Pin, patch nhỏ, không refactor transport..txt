Tiếp tục từ V16.0.299. Sửa riêng TP/Test Pin, patch nhỏ, không refactor transport.

1. TP phải hoạt động với MỌI IO vật lý trong active board capacity:
   - IO có mapping trong THT -> hiện TP + Connector/Pin nếu có.
   - IO không có mapping trong THT -> vẫn hiện TP + số IO, mapping để trống.
   - KHÔNG yêu cầu ProductModel.Pins / expected network để nhận TP.

2. Khi đầu dò RELEASE:
   - clear ngay TP preview/current contact;
   - xóa toàn bộ TP row/overlay transient;
   - restore ngay màn hình Production gốc:
     state = WaitingForProduct / "LẮP SẢN PHẨM";
   - restore bảng CHƯA KẾT NỐI theo model hiện tại;
   - không chờ product-removal debounce vì TP chưa bao giờ là product.
   Target: cùng UI update sau RELEASE, không stale Dispatcher callback.

3. Nếu file THT TRỐNG:
   - vẫn cho START/TestView;
   - vẫn scan toàn bộ active/installed board capacity;
   - vẫn nhận TP trên mọi IO như Htdrv3 gốc;
   - hiển thị ít nhất "TP - IOxx";
   - không PASS, không FAIL, không ProductDetected vì ExpectedNetCount=0;
   - không yêu cầu Connector/Pin/Wire mapping.
   Empty model = TP-only usable mode, KHÔNG phải disabled tester.

4. Probe/TP frame tuyệt đối không được feed vào Wrong/Short/Product evidence.
   Log hiện có case raw_active_count=1 nhưng wrong_candidates=2 -> state=Present.
   Audit và chặn race này:
   nếu frame/contact đang được ProbeContactClassifier xác nhận là TP/probe transition
   thì không tạo WRONG_CANDIDATE, ProductPresent, PRODUCT_START_SOUND hoặc FAIL giả.

5. Giữ behavior:
   shell/raw single-IO không phải TP hợp lệ -> no-op Production.
   TP hợp lệ -> chỉ TP display.
   Product thật -> Production connectivity như cũ.
   Wrong/Short thật của sản phẩm -> vẫn bắt realtime.

6. Test bắt buộc:
   - mapped IO TP -> hiện, release -> reset;
   - unmapped IO TP -> hiện IO, release -> reset;
   - empty THT + IO bất kỳ -> TP hoạt động;
   - empty THT không PASS/FAIL/Product;
   - TP không tạo Wrong/Short/Product;
   - stale TP UI callback sau release bị bỏ;
   - Product thật/Wrong thật không regression.

Không sửa Resistance, relay, ScanSupervisor, D2XX, DB/LOT/label.
Bump version và báo build/self-test.