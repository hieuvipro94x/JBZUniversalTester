TIẾP TỤC TỪ WORKTREE HIỆN TẠI TRÊN BRANCH:

wip-codex-20260829

QUAN TRỌNG:
KHÔNG hoàn tác bất kỳ thay đổi hiện tại nào.

Bốn file sau đã được sửa thủ công và đã copy vào project từ một bản fix riêng:

- Services/D2xxBoardTransport.cs
- ViewModels/TestViewModel.cs
- HardwareVerification/Program.cs
- Tests/Program.cs

KHÔNG sửa lại logic trong 4 file này trừ khi:
1. build hiện tại thật sự báo compile error xuất phát trực tiếp từ chúng;
2. hoặc self-test thất bại và có bằng chứng rõ lỗi nằm trong đúng file đó.

Nếu buộc phải sửa một trong 4 file trên:
- chỉ sửa tối thiểu để build/test pass;
- không refactor;
- không hoàn tác AppliedScanCapacity;
- không hoàn tác requested-vs-applied reconciliation;
- không hoàn tác multi-card fixes;
- phải báo rõ trước/sau đã thay dòng logic nào.

============================================================
TRẠNG THÁI HIỆN TẠI ĐÃ XÁC NHẬN
============================================================

Sau khi copy 4 file fix vào project:

dotnet build -c Release

đã PASS:

JBZUniversalTester
net8.0-windows win-x86

JBZUniversalTester.SelfTests
PASS

JBZUniversalTester.HardwareVerification
PASS

Self-tests:

34/34 PASS.

Không được làm regression kết quả này.

============================================================
NHỮNG PHẦN ĐÃ FIX — KHÔNG LÀM LẠI
============================================================

Đã xử lý:

- AppliedScanCapacity lifecycle.
- requested capacity khác applied capacity → restart firmware đúng một lần.
- requested capacity == applied capacity → reuse scan, không STOP/START.
- background scan active=1 rồi model yêu cầu active=8 → reconcile đúng.
- load model reconcile active scan capacity.
- HardwareVerification hỗ trợ active expansion range đúng hơn.
- FakeBoard/self-test phản ánh complete production frame.
- multi-card regression tests đã có.
- database không bị đụng.

KHÔNG viết lại những phần trên.

============================================================
NHIỆM VỤ CỦA CODEX LẦN NÀY
============================================================

Chỉ audit và hoàn thiện CÁC PHẦN CÒN LẠI ngoài 4 file đã khóa.

Trọng tâm:

1. UI PERFORMANCE
2. MEMORY LEAK
3. SETTINGS PAGE
4. HISTORY UI
5. MAINWINDOW / TESTWINDOW LIFETIME
6. RESOURCE DICTIONARY
7. TIMER / EVENT SUBSCRIPTIONS
8. OUT OF MEMORY HANDLING
9. LOW-RAM / LOW-CPU COMPATIBILITY
10. STARTUP FIRST-RENDER
11. LOGGING / DISPATCHER PRESSURE
12. BUILD / REGRESSION

DATABASE vẫn đang làm dở:
KHÔNG REFACTOR DATABASE.

============================================================
FILE PERFORMANCE/MEMORY ĐƯỢC PHÉP AUDIT/SỬA
============================================================

Ưu tiên:

- App.xaml.cs
- Views/MainWindow.xaml.cs
- Views/ProductionSettingsPage.xaml
- Views/ProductionSettingsPage.xaml.cs
- Views/HistoryPage.xaml
- Views/HistoryPage.xaml.cs
- Views/TestWindow.xaml
- Views/TestWindow.xaml.cs
- App.xaml
- các ResourceDictionary dùng chung
- các UI helper/service thật sự liên quan memory/lifecycle

Nếu cần sửa file khác:
phải chứng minh file đó gây UI/memory issue.

============================================================
OUT OF MEMORY ISSUE
============================================================

Đã từng có crash:

System.OutOfMemoryException

stack liên quan:

System.Xaml.XamlNodeList
Baml2006Reader
ResourceDictionary
FindImplicitStyleResource
XamlReader.LoadBaml
Application.LoadComponent
ProductionSettingsPage.InitializeComponent
MainWindow.ShowSettingsPage

Do đó phải audit:

ProductionSettingsPage.xaml
ProductionSettingsPage.xaml.cs
App.xaml
shared resources
MainWindow navigation/page lifetime.

Không coi lỗi này là USB/board/D2XX error.

============================================================
SETTINGS PAGE MEMORY
============================================================

Search trong Settings page và related UI:

+=
PropertyChanged +=
CollectionChanged +=
Loaded +=
Unloaded +=
DispatcherTimer
Timer
FileSystemWatcher
static event
commands retaining page.

Đảm bảo:

open Settings
close Settings

không giữ page cũ sống.

Nếu recreate page:
phải Detach/Dispose đúng.

Nếu cache page:
chỉ một instance.

Không vừa recreate vừa leak instance cũ.

============================================================
RESOURCE DICTIONARY
============================================================

Kiểm tra:

Style
ControlTemplate
DataTemplate
Brush
Geometry
Converter
DropShadowEffect
MergedDictionaries

Không tạo cùng resource dictionary nhiều lần cho mỗi page.

Common resources nên shared ở application-level nếu hợp lý.

Không đổi giao diện lớn nếu không cần.

============================================================
LOW-RAM OPTIMIZATION
============================================================

Không tối ưu bằng cách làm chậm TestEngine.

Chỉ giảm:

- UI allocations
- duplicate page instances
- history rows trong memory
- log rows
- effects/animation không cần
- duplicate timers
- duplicate watchers
- Dispatcher callbacks.

Không giảm:

- hardware frames
- SHORT detection
- WRONG detection
- Probe sensitivity
- CLIP
- Master
- resistance/leak logic.

============================================================
HISTORY UI
============================================================

Hiện đã có:

- giới hạn khoảng 2.000 rows;
- recycling virtualization.

Audit giữ đúng.

Không refactor database/query architecture.

Chỉ sửa UI lifecycle/virtualization nếu còn lỗi.

============================================================
DATAGRID
============================================================

Audit:

EnableRowVirtualization
EnableColumnVirtualization
VirtualizingPanel.IsVirtualizing
VirtualizationMode=Recycling

Không bọc DataGrid trong ScrollViewer ngoài nếu làm mất virtualization.

============================================================
MAINWINDOW
============================================================

First render phải xảy ra trước heavy startup.

Không heavy I/O trong constructor/Loaded nếu chưa cần.

Không tạo Settings/History/TestWindow trước khi người dùng mở.

============================================================
TESTWINDOW
============================================================

Không initialize board lần hai.

Không giữ event handler sau close.

Không dispose D2XX khi chỉ đóng TestWindow.

Không sửa logic trong TestViewModel trừ compile/test bắt buộc.

============================================================
EVENT / TIMER OWNERSHIP
============================================================

Mỗi page/window phải có owner rõ.

Khi close/unload:

unsubscribe.

Không để:

page A mở/đóng 50 lần
→ 50 timers
→ 50 PropertyChanged handlers.

============================================================
LOG UI
============================================================

Không giữ log UI vô hạn.

Giới hạn hợp lý:

100–300 dòng.

Không raw D2XX log vào Dispatcher liên tục.

Không sửa D2xxBoardTransport vì file đang khóa.

Chỉ tối ưu presentation/log collection nếu cần.

============================================================
OUTOFMEMORY HANDLING
============================================================

App-level exception handler không được:

e.Handled = true

cho mọi OutOfMemoryException rồi tiếp tục UI.

OOM phải:

- ghi đúng loại lỗi;
- ghi crash report nếu hệ thống hiện có;
- safe shutdown best effort;
- không báo nhầm kiểm tra USB/bo.

Không redesign crash/database persistence.

============================================================
BUILD PLATFORM
============================================================

GIỮ:

win-x86

Không chuyển x64.

Không thay Prefer32Bit/platform target trong task này.

============================================================
DATABASE — KHÓA
============================================================

Không refactor:

TestHistoryStore
ProductionStatisticsStore
PartCounterStore
LegacyPhtHistoryReader
LegacyPhtHistoryService
SQLite schema
migration
production statistics architecture.

Database đang là work-in-progress riêng.

Nếu thấy lỗi:
REPORT ONLY.

Không sửa trừ compile blocker thật sự.

============================================================
STATIC AUDIT
============================================================

Search:

Thread.Sleep
.Result
.Wait()
GetAwaiter().GetResult()
Dispatcher.Invoke
Dispatcher.BeginInvoke
new ProductionSettingsPage
new HistoryPage
new TestWindow
DispatcherTimer
FileSystemWatcher
PropertyChanged +=
CollectionChanged +=
Loaded +=
Unloaded +=
MergedDictionaries
DropShadowEffect

Không blindly replace.

============================================================
MEMORY STRESS TARGET
============================================================

Sau sửa phải hướng tới:

open/close Settings 100 lần
→ memory plateau

open/close TestWindow 100 lần
→ no thread/event leak

open History repeatedly
→ no linear memory growth

normal production
→ memory không tăng tuyến tính theo số cycle.

Nếu không thể đo trong environment:
ghi rõ NOT MEASURED.

============================================================
TRƯỚC KHI EDIT
============================================================

In:

PERFORMANCE/MEMORY AUDIT

1. Settings page lifetime:
...

2. History page lifetime:
...

3. MainWindow navigation:
...

4. TestWindow lifetime:
...

5. Resource dictionaries:
...

6. Timers/watchers:
...

7. Fatal exception handling:
...

8. Files to modify:
...

LOCKED FILES:
- Services/D2xxBoardTransport.cs
- ViewModels/TestViewModel.cs
- HardwareVerification/Program.cs
- Tests/Program.cs

DATABASE:
EXCLUDED.

Sau đó mới sửa.

============================================================
SAU KHI SỬA
============================================================

Chạy:

dotnet build -c Release

Phải PASS.

Sau đó:

dotnet run --project Tests\JBZUniversalTester.SelfTests.csproj -c Release --no-build

Phải tối thiểu:

34/34 PASS.

Không xóa test cũ.

Sau đó:

git diff --check

Phải sạch.

============================================================
REPORT CUỐI
============================================================

FILES MODIFIED

...

LOCKED FILES MODIFIED:
NONE

hoặc nếu bắt buộc:
ghi rõ lý do.

PERFORMANCE FIXES

...

MEMORY LEAK FIXES

...

SETTINGS PAGE FIXES

...

HISTORY UI FIXES

...

OUT OF MEMORY HANDLING

...

BUILD:
PASS/FAIL

SELF TEST:
xx/xx PASS

DATABASE:
UNCHANGED

MULTI-CARD FIX:
PRESERVED

HARDWARE TEST:
NOT PERFORMED nếu không có FTDI thật.

============================================================
MỤC TIÊU
============================================================

Hoàn thiện phần performance/memory/UI quanh baseline hiện tại mà KHÔNG đụng lại 4 file multi-card đã được sửa thủ công.

Không làm lại công việc đã hoàn thành.

Không sửa database.

Không thay hardware semantics.

Chỉ tối ưu UI/resource/lifecycle để app chạy ổn hơn trên máy RAM thấp và CPU thấp.