JBZ Universal Tester V10.9 - AUTO STARTUP + PINPROBE CLOSE FIX

1. MainWindow Loaded tự khởi tạo toàn bộ:
   - tự kết nối/recovery FTDI board, không chờ bấm Bắt đầu test;
   - tự nạp model gần nhất ngay trên MainWindow;
   - MainWindow hiển thị model và trạng thái board trước khi test.

2. Model được lưu ngay khi người dùng CHỌN MÃ HÀNG, không chờ tới Start Test.
   Vì vậy mở app lần sau sẽ nạp đúng mã đang dùng gần nhất.

3. Kết nối board không cache vĩnh viễn lần lỗi. Nếu startup chưa kết nối được,
   Start/TestPin sẽ tự recovery và thử lại mà không cần nút Kết nối.

4. Fix InvalidOperationException PinProbeWindow line 98:
   không gọi Close() re-entrant từ async Window.Closing; dừng Probe, nhường Dispatcher,
   rồi Close ở lượt message mới.

5. Bấm BẮT ĐẦU KIỂM TRA chỉ bắt đầu production scan. Board/model đã sẵn sàng từ startup.
