<Window x:Class="JBZUniversalTester.Views.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ĐĂNG KÝ PHẦN MỀM"
        Width="660" Height="470"
        MinWidth="560" MinHeight="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ShowInTaskbar="True">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="JBZ UNIVERSAL TESTER" FontSize="28" FontWeight="Bold"
                   Foreground="#273F91" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="ĐĂNG KÝ PHẦN MỀM" FontSize="20" FontWeight="SemiBold"
                   Margin="0,8,0,26" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="2">
            <TextBlock Text="ID ĐĂNG KÝ" FontWeight="Bold" Margin="0,0,0,6"/>
            <TextBox x:Name="RegistrationIdTextBox" IsReadOnly="True" FontSize="18"
                     FontFamily="Consolas" HorizontalContentAlignment="Center"/>
            <Button Content="SAO CHÉP ID" Click="CopyId_Click" HorizontalAlignment="Left"
                    MinWidth="150" Margin="0,10,0,20"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="MÃ ĐĂNG KÝ" FontWeight="Bold" Margin="0,0,0,6"/>
            <TextBox x:Name="ActivationCodeTextBox" MinHeight="92" AcceptsReturn="True"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="4" Margin="0,16,0,10"
                   TextAlignment="Center" FontSize="16" FontWeight="Bold"
                   Foreground="#C62828" TextWrapping="Wrap"/>

        <Button Grid.Row="5" Content="ĐĂNG KÝ" Click="Activate_Click"
                HorizontalAlignment="Center" MinWidth="190" IsDefault="True"
                Background="#273F91" Foreground="White"/>
    </Grid>
</Window>
