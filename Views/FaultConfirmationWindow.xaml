<Window x:Class="JBZUniversalTester.Views.FaultConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xử lý hàng không đạt"
        Width="720" Height="470"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F3F4F6"
        FontFamily="Segoe UI"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#B91C1C" CornerRadius="8" Padding="14,10">
            <TextBlock Text="XỬ LÝ HÀNG KHÔNG ĐẠT" Foreground="White"
                       HorizontalAlignment="Center" FontSize="25" FontWeight="Bold"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="0,16,0,10">
            <TextBlock x:Name="FaultTitleText" Text="KIỂM TRA MẠCH KHÔNG ĐẠT"
                       HorizontalAlignment="Center" FontSize="22" FontWeight="Bold" Foreground="#991B1B"/>
            <TextBlock x:Name="SummaryText" Margin="0,8,0,0" HorizontalAlignment="Center"
                       FontSize="16" FontWeight="SemiBold" Foreground="#374151"/>
        </StackPanel>

        <Border Grid.Row="2" Background="White" BorderBrush="#D1D5DB" BorderThickness="1"
                CornerRadius="6" Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="DetailsText" TextWrapping="Wrap" FontSize="16" LineHeight="24"/>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="230"/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Sau khi xác nhận, phần mềm sẽ tự đưa JIG về trạng thái tháo hàng an toàn."
                       FontSize="14" Foreground="#4B5563" TextWrapping="Wrap" Margin="0,0,14,0"/>
            <Button Grid.Column="1" Height="58" Content="XÁC NHẬN"
                    FontSize="18" FontWeight="Bold" IsDefault="True"
                    Background="#F59E0B" Foreground="#111827" Click="Confirm_Click"/>
        </Grid>
    </Grid>
</Window>
