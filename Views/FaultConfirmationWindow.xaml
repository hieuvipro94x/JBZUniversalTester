<Window x:Class="JBZUniversalTester.Views.FaultConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xử lý hàng không đạt"
        Width="780" Height="590"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F3F4F6"
        FontFamily="Segoe UI"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#B91C1C" CornerRadius="8" Padding="14,10">
            <TextBlock Text="XỬ LÝ HÀNG KHÔNG ĐẠT" Foreground="White"
                       HorizontalAlignment="Center" FontSize="25" FontWeight="Bold"/>
        </Border>

        <TextBlock x:Name="SummaryText"
                   Grid.Row="1"
                   Margin="0,14,0,10"
                   HorizontalAlignment="Center"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="#991B1B"/>

        <Border Grid.Row="2" Background="White" BorderBrush="#D1D5DB" BorderThickness="1"
                CornerRadius="6" Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="FaultItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,10"
                                    Padding="12,10"
                                    Background="#FFF7F7"
                                    BorderBrush="#E5B7B7"
                                    BorderThickness="1"
                                    CornerRadius="5">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="18"
                                               FontWeight="Bold"
                                               Foreground="#991B1B"/>
                                    <ItemsControl Margin="0,7,0,0" ItemsSource="{Binding Lines}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="190"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Label}"
                                                               FontSize="15"
                                                               FontWeight="SemiBold"
                                                               Foreground="#374151"/>
                                                    <TextBlock Grid.Column="1"
                                                               Text="{Binding Value}"
                                                               FontSize="15"
                                                               Foreground="#111827"
                                                               TextWrapping="Wrap"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="230"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="FooterText" VerticalAlignment="Center"
                       FontSize="14" Foreground="#4B5563" TextWrapping="Wrap" Margin="0,0,14,0"/>
            <Button Grid.Column="1" Height="58" Content="XÁC NHẬN"
                    FontSize="18" FontWeight="Bold" IsDefault="True"
                    Background="#F59E0B" Foreground="#111827" Click="Confirm_Click"/>
        </Grid>
    </Grid>
</Window>
