<UserControl x:Class="JBZUniversalTester.Views.HistoryPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="#EEF1F4"
        PreviewKeyDown="Page_PreviewKeyDown">

    <UserControl.Resources>
        <Style x:Key="CompactButton"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="MinWidth" Value="86"/>
            <Setter Property="Padding" Value="14,5"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="FilterTextBox"
               TargetType="TextBox"
               BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="FilterLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#5B6470"/>
            <Setter Property="Margin" Value="1,0,0,3"/>
        </Style>

        <Style x:Key="HistoryHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="MinWidth" Value="86"/>
            <Setter Property="Background" Value="#E8EDF3"/>
            <Setter Property="Foreground" Value="#293241"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#C8D0D9"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" TextAlignment="Center"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CenterCellText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="White"
                BorderBrush="#D2D8DF"
                BorderThickness="1"
                CornerRadius="3"
                Padding="12,8"
                Margin="0,0,0,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="115"/>
                    <ColumnDefinition Width="115"/>
                    <ColumnDefinition Width="115"/>
                    <ColumnDefinition Width="310"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="LỊCH SỬ KIỂM TRA"
                               FontSize="19"
                               FontWeight="Bold"
                               Foreground="#243447"/>
                    <TextBlock Text="Tra cứu kết quả theo ngày, LOTNO và mã hàng"
                               FontSize="10.5"
                               Foreground="#7A8490"
                               Margin="0,1,0,0"/>
                </StackPanel>

                <Border Grid.Column="1" Background="#F2F5F8" CornerRadius="3" Margin="3,0" Padding="7,3">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="TỔNG" FontSize="9.5" Foreground="#69737D" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TotalCountText" Text="0" FontSize="17" FontWeight="Bold" Foreground="#22313F" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="#EEF8F1" CornerRadius="3" Margin="3,0" Padding="7,3">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="PASS" FontSize="9.5" Foreground="#3E6D4D" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="PassCountText" Text="0" FontSize="17" FontWeight="Bold" Foreground="#16813A" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="3" Background="#FFF0F0" CornerRadius="3" Margin="3,0" Padding="7,3">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="FAIL" FontSize="9.5" Foreground="#8A4B4B" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="FailCountText" Text="0" FontSize="17" FontWeight="Bold" Foreground="#C62828" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="4"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="10,0,0,0">
                    <Button Content="XUẤT CSV"
                            Width="100"
                            Style="{StaticResource CompactButton}"
                            Click="ExportCsv_Click"/>
                    <Button Content="XUẤT XLSX"
                            Width="105"
                            Style="{StaticResource CompactButton}"
                            Click="ExportXlsx_Click"
                            Background="#DDEFD9"
                            BorderBrush="#8AB681"/>
                    <Button Content="ĐÓNG"
                            Width="82"
                            Style="{StaticResource CompactButton}"
                            Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Compact filters -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#D2D8DF"
                BorderThickness="1"
                CornerRadius="3"
                Padding="9,7"
                Margin="0,0,0,6">
            <WrapPanel VerticalAlignment="Center" ItemHeight="58">
                <StackPanel Width="150" Margin="0,0,10,4">
                    <TextBlock Text="Từ ngày" Style="{StaticResource FilterLabel}"/>
                    <DatePicker x:Name="FromDatePicker" Height="30" FontSize="12"/>
                </StackPanel>

                <StackPanel Width="150" Margin="0,0,10,4">
                    <TextBlock Text="Đến ngày" Style="{StaticResource FilterLabel}"/>
                    <DatePicker x:Name="ToDatePicker" Height="30" FontSize="12"/>
                </StackPanel>

                <StackPanel Width="115" Margin="0,0,10,4">
                    <TextBlock Text="LOTNO" Style="{StaticResource FilterLabel}"/>
                    <TextBox x:Name="LotTextBox" Style="{StaticResource FilterTextBox}"/>
                </StackPanel>

                <StackPanel Width="245" Margin="0,0,10,4">
                    <TextBlock Text="Mã hàng / tên hàng" Style="{StaticResource FilterLabel}"/>
                    <TextBox x:Name="PartTextBox" Style="{StaticResource FilterTextBox}"/>
                </StackPanel>

                <StackPanel Width="235" Margin="0,0,10,4">
                    <TextBlock Text="Kết quả" Style="{StaticResource FilterLabel}"/>
                    <ComboBox x:Name="ResultComboBox"
                              Height="30"
                              FontSize="12"
                              SelectedIndex="0">
                        <ComboBoxItem Content="ALL"/>
                        <ComboBoxItem Content="PASS"/>
                        <ComboBoxItem Content="FAIL"/>
                        <ComboBoxItem Content="DÂY CHƯA KẾT NỐI"/>
                        <ComboBoxItem Content="ĐẤU SAI"/>
                        <ComboBoxItem Content="CHẬP MẠCH"/>
                        <ComboBoxItem Content="ĐIỆN TRỞ KHÔNG ĐẠT"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="2,17,0,4">
                    <Button Content="TÌM KIẾM"
                            Width="106"
                            Style="{StaticResource CompactButton}"
                            Click="Search_Click"
                            Background="#DDEBF7"
                            BorderBrush="#83A9C7"
                            FontWeight="Bold"/>
                    <Button Content="XÓA LỌC"
                            Width="92"
                            Style="{StaticResource CompactButton}"
                            Click="ClearFilter_Click"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- History table -->
        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#C8D0D9"
                BorderThickness="1"
                CornerRadius="2">
            <DataGrid x:Name="HistoryGrid"
                      ItemsSource="{Binding Records}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserResizeRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      RowHeight="32"
                      ColumnHeaderHeight="40"
                      FontSize="11.5"
                      Background="White"
                      BorderThickness="0"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow"
                      AlternatingRowBackground="#F8FAFC"
                      RowBackground="White"
                      FrozenColumnCount="2"
                      ColumnHeaderStyle="{StaticResource HistoryHeaderStyle}"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderBrush" Value="#E1E6EB"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Passed}" Value="True">
                                <Setter Property="ToolTip" Value="Kết quả PASS"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Timestamp"
                                        Binding="{Binding Finished, StringFormat={}{0:yyyy/MM/dd HH:mm:ss}}"
                                        Width="155"/>
                    <DataGridTextColumn Header="Part Name" Binding="{Binding PartName}" Width="150" MinWidth="130"/>
                    <DataGridTextColumn Header="Part Number" Binding="{Binding PartNumber}" Width="150" MinWidth="130"/>
                    <DataGridTextColumn Header="ECO" Binding="{Binding Eco}" Width="95"/>
                    <DataGridTextColumn Header="NCO" Binding="{Binding Nco}" Width="90"/>
                    <DataGridTextColumn Header="ALC" Binding="{Binding Alc}" Width="100"/>
                    <DataGridTextColumn Header="Lot Number" Binding="{Binding LotNo}" Width="100" MinWidth="95" ElementStyle="{StaticResource CenterCellText}"/>

                    <DataGridTemplateColumn Header="Test Result" Width="115" MinWidth="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PassedText}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#C62828"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Passed}" Value="True">
                                                    <Setter Property="Foreground" Value="#15803D"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Fault Type" Binding="{Binding CustomerFault.FaultType}" Width="205"/>
                    <DataGridTextColumn Header="Fault Location" Binding="{Binding CustomerFault.FaultLocation}" Width="210"/>
                    <DataGridTextColumn Header="Standard" Binding="{Binding CustomerFault.Standard}" Width="280"/>
                    <DataGridTextColumn Header="Actual" Binding="{Binding CustomerFault.Actual}" Width="210"/>
                    <DataGridTextColumn Header="Deviation" Binding="{Binding CustomerFault.Deviation}" Width="120"/>
                    <DataGridTextColumn Header="Assessment" Binding="{Binding CustomerFault.Assessment}" Width="190"/>
                    <DataGridTextColumn Header="Software Version" Binding="{Binding HtdrvName}" Width="260"/>
                    <DataGridTextColumn Header="Open Count" Binding="{Binding OpenCount}" Width="105" MinWidth="95" ElementStyle="{StaticResource CenterCellText}"/>
                    <DataGridTextColumn Header="Incorrect Connection Count" Binding="{Binding WrongCount}" Width="205" MinWidth="185" ElementStyle="{StaticResource CenterCellText}"/>
                    <DataGridTextColumn Header="Short Circuit Count" Binding="{Binding ShortCount}" Width="155" MinWidth="145" ElementStyle="{StaticResource CenterCellText}"/>
                    <DataGridTextColumn Header="Resistance" Binding="{Binding Resistance}" Width="180"/>
                    <DataGridTextColumn Header="Test Station" Binding="{Binding DeviceName}" Width="130"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding ModelName}" Width="125"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Status bar -->
        <Grid Grid.Row="3" Margin="2,4,2,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="310"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="SummaryText"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="#66717C"
                       FontSize="10.5"
                       TextTrimming="CharacterEllipsis"/>

            <TextBlock Grid.Column="1"
                       Text="Enter: tìm kiếm"
                       VerticalAlignment="Center"
                       Foreground="#9098A1"
                       FontSize="10"
                       Margin="12,0,2,0"/>
        </Grid>
    </Grid>
</UserControl>
