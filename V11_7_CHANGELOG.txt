JBZUniversalTester V11.7 - PROBE CONTACT / PRODUCTION FAULT ISOLATION

1. PinProbeWindow khóa Production ngay từ constructor, trước Loaded/ShowDialog.
2. Callback/popup Production dùng generation + probeSession guard; callback cũ không thể hiện popup trong Probe.
3. TestPin I/O không có map THT vẫn hiển thị duy nhất IO(n), không báo lỗi/chập.
4. TestPin I/O có map hiển thị IO(n), giắc, chân, tên dây, màu, tiết diện và I/O liên quan theo THT.
5. Khi nhả que, dòng Probe biến mất ở frame rỗng kế tiếp.
6. WrongWiring/Short chỉ được tạo/hiện popup trong RuntimeMode.Production.
7. Đấu sai Production vẫn báo ngay frame sai đầu tiên, không chờ ĐANG KIỂM TRA/1000 ms.
8. Sửa BuildNetsByPin dùng Dictionary typed trực tiếp, tránh lỗi generic ToDictionary/ReferenceEqualityComparer.
9. Xóa warning _haveFrameBoundary không dùng.
